{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <article>
        <div>
            <h1>Mon profil</h1>

            <aside>
                <img src="{{ asset('img/photoProfil/' ~user.picture) }}" alt="Photo de profil">
            </aside>

            {{ form_start(userForm) }}
            {{ form_widget(userForm) }}

            <button type="submit">Enregistrer</button>
            <a href="{{ path('main_home') }}">Annuler</a>
            {{ form_end(userForm) }}

            <a href="{{ path('app_utilisateur_user_editpassword', {'id': app.user.id}) }}">Modifier le mot de passe</a>
        </div>
        <div>
        </div>
    </article>
{% endblock %}